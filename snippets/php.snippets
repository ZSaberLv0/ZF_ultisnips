# ============================================================
snippet cat "string concat"
	.
# ============================================================
snippet for "for loop"
	for(\$${1:i} = ${2:0}; \$${1} < ${3:N}; ++\$${1}) {
			${4}
	}${0}
# ============================================================
snippet foreach "foreach loop"
	foreach(\$${1:arr} as \$${2:k => \$v}) {
		${3}
	}${0}
# ============================================================
